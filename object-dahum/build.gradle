
dependencies {
    if(rootProject.findProject('core-dahum')) // core에 의존
        compile project(':core-dahum')

    // gson (JSON parser)
    compile group: 'com.google.code.gson', name: 'gson', version: '2.8.6'



}
